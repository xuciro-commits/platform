# Work Queue

The only list of active platform work. Each item: goal, boundary, done-when, status. When an item is done, delete it (git history keeps it) and fold lasting knowledge into `docs/Platform.md`. The target apps are CRM, MES and ERP (Intent.md); Music is no longer a target.

The order comes from the stage review of 2026-09-26 (Platform.md §9, §10.3, §10.5): first the engineering items that keep verification honest (CI, #112, done), then stage 6 (built), then the ERP (#115, stage 7, built), then one shape for every app and the host's boundary (ADR-0025: #117, then #113). The owner's testing and the external review of 2026-09-26 (Platform.md §10.5) set what follows: the return path to people first (#118), then decisions across apps (#119, built), with #113 in between, then one runtime for durable work (#120, a gate, next), then stage 7.

| # · Status | Task | Done when |
|---|---|---|
| **118 · built, owner testing** | The return path (Platform.md §10.5): what happens reaches the people and pages concerned. A task's notifications are read when it closes (F-30); whoever is notified may open what the notification refers to (F-31); the host streams changes and the workspace refetches what is on screen (F-32); a flow step that fails without a `Fault` tells the flow's owners and gives them a task instead of ending silently; generated views offer every declared action and hand-written views only add (F-33), and a transition's payload is asked in a form (F-27). Built and walked in the browser: the leave and CRM scenarios | The owner walks docs/Testing.md routes 4 and 17 |
| **119 · built, owner testing** | Decisions across apps (ADR-0026, accepted on delegation; 9a to 9c built): one decision changes one app — rules probe another app, and once accepted its requests run at the provider and each answer comes back to the app's own reply action, which a person takes when no provider is bound; holds until a date, then confirm or release (`lodging.booking/1`); a record's state follows its process and its page lists the flows about it. The CRM's group block (plan holds, won confirms, lost or past the cutoff releases) and the MES's confirmation to the ERP run on it. Walked in the browser: plan, win, a refused plan, a failed block | The owner walks docs/Testing.md route 5 |
| **120 · gate, next** | One runtime for durable work (K9 grown from ownership into execution): flows, effects, jobs, agents and connectors on one scheduler — queues, timers, retries with backoff, breakers, checkpoints, fairness and quotas per tenant and app (ADR-0015 batch 2's quotas join it); OpenTelemetry traces and metrics and the health of apps and the journal instrumented in the same place | The owner has decided; a failing endpoint no longer blocks others; one tenant cannot starve another in a test; traces follow a submission through a flow and an effect |
| **111 · built, owner testing** | Stage 5, agents (ADR-0021, ADR-0022): declared agents, runs, drafts, signals, evaluation, the assistant, run pages and search; customer service (CSM); knowledge with citations, memory, A2A, transcripts (batches 1 to 3c) | The owner has walked docs/Testing.md routes 6 to 10 |
| **105 · batch 1 done, owner testing** | AI providers (ADR-0015): providers, catalogs, enabled models with access, calls with journaled usage, Settings | The owner has tested with OpenRouter; batch 2 moves into stage 8 |
| **117 · built, owner testing** | One shape for every app (ADR-0025): industry names (PMS, HCM, CSM, MES, the ERP adapter; solutions `hospitality` and `manufacturing`), one layout for server and web written by the scaffold and checked, every app standalone first with outside intake through its declared actions, inputs and protocols (8a built: the ADR, Swift deleted; 8b built: names, layout, a development host for every app) | The owner has walked the renamed routes and run an app alone on its development host |
| **113 · accepted, 8c in progress** | The host's own apps as apps (ADR-0025 D4, batch 8c; `relations`, `org` and `work` moved, then `flow`, `agent`, `knowledge`, `ai`, `platform`): each platform app a package on the app API plus `internal/host`, called through roles, with one event path; split Settings (`web/packages/platform/src/index.tsx`) by area | `scripts/boundaries.sh` forbids platform apps from importing the host; every composition's tests and the rehearsal pass |
| **114 · built, owner testing** | Stage 6 (ADR-0023, accepted): languages (6a built: switch to 简体中文 in the profile menu), the semantic model (descriptions, examples, synonyms declared on entity types, fields, states and actions; served by `/v1/entities`; used by agents, MCP, A2A cards, search and forms), the host API contract (OpenAPI generated from routes and manifests; TypeScript clients generated from it), and the developer kit (app guide, scaffold, skills) | The owner has walked route 11 in Chinese and scaffolded an app (route 13) |
| **115 · built, owner testing** | The ERP target app (ADR-0024, accepted on the owner's delegation): accounting (7a built: accounts, journal entries posted with gapless numbers, reversal, periods, trial balance; number sequences for every app), purchasing and inventory (7b built: orders with approval, receipts and bills posted, on hand), production orders the MES executes through `production.orders/1` (7c built: `solutions/plant`), the adapter to an ERP outside so the plant has one path to any ERP (7d built: `apps/erplink`; Docker and the rehearsal on `plant-server`). Thin: the pressures are money, units, posting, numbers and periods. It is the first proof of stage 7 | The owner has walked routes 1, 6, 14, 15 and 16 |

## Open friction (temporary; delete entries once resolved)

| ID | Domain | Kind | Observation | Resolution path |
|---|---|---|---|---|
| F-34 | Host | limit (ADR-0007) | `Journal.Append` holds one lock across every tenant while it writes to PostgreSQL, so tenants append one after another | A lock per tenant (order is per tenant); whether one total order per tenant stays is the partitioning gate before stage 9 |
| F-37 | Workspace | limit (ADR-0016) | A record's history shows a change to a lines field (the CRM's rooms) as two JSON arrays, and a timeline note keeps the protocol event's English title | Show lines changes row by row; say a note's event title in the reader's language |
| F-36 | Workspace | missing (ADR-0016) | An action's payload fields are typed but carry no choices or references: generated action forms ask "won or lost" and an account's ID as free text | Payload fields declare choices and a referred entity type, as entity fields do, and the form offers them |
| F-35 | Workspace | limit (#118) | Browser smoke tests of the routes need a new dependency (Playwright) the owner approves; until then routes are walked by hand in the Browser pane | Approve the dependency, then a smoke test per route in `scripts/verify.sh web` |
| F-29 | ERP | missing (ADR-0014 D6) | What cannot be recalled is held for a person only when it is an effect: through the ERP link, an AI agent's confirmation waits for approval, but through the ERP app the same confirmation is a protocol action and posts at once if the agent holds a role there (today none does, so the ERP refuses it) | Hold a protocol action an agent causes when the provider declares it irreversible, as D6 holds effects |
| F-28 | ERP | limit (ADR-0011, ADR-0014 D4) | A provider hears an answer from outside through `Answerer` and records it with `PutAt`, which is no decision, so it cannot be a protocol event: the plant's confirmation flow learns the ERP link's answer by checking the provider's read every second (`awaited`) and records it with its own action (`mes.order.answer`) | An answer an app records may be declared an event of a protocol it provides, delivered like a decision's |
| F-26 | ERP | limit (ADR-0016) | A record's history shows a changed lines field as its JSON before and after: posting an entry shows every line with its money objects | The kit shows changed lines as rows added, removed and changed |
| F-25 | ERP | limit (ADR-0023) | A generated description is said through the first split a pattern finds: a field's help containing ": " or " (" cannot be said ("Kind: Where …: …"), so the ERP rephrased its help | Try each split a pattern allows, or say generated descriptions from their parts instead of their English |
| F-24 | CSM | missing (ADR-0014) | An app does not hear that a person discarded an effect it emitted: a discarded agent reply leaves the ticket "answered" though nothing was mailed (the run gets a `discarded` signal, the app nothing) | Hand `Answerer` a `discarded` outcome, journaled like the others, so the helpdesk reopens the ticket |
| F-23 | Manufacturing | missing (K6 errors) | A refused decision carries only its code: a supervisor who resends an order against a planned order for another product sees "invalid argument", not "PO-9001 is for P-100, not P-200" (the plant knows why, `fits`) | A kernel rule for a refusal's reason (spec and vectors first), shown by the generated form; or the assistant offers only fitting planned orders |
| F-22 | Composition | limit (K4 C3) | Causation cannot name a change in another package's log; a protocol call (the CRM reserving a stay) derives the provider's idempotency key from the consumer's and puts the consumer's key in the correlation ID | Keep derived keys unless audit needs cross-log causation; then a contract rule with vectors |
