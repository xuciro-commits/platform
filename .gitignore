.build/
**/.build/
.DS_Store
target/
apps/pms/client/src-tauri/gen/
# Build outputs of `go build` and `go test -c` in a module directory
*.test
# every app's and solution's development host, and the local tools
apps/*/server/cmd/*/*-server
apps/*/server/cmd/*/*-sim
apps/*/server/cmd/*/*-agent
solutions/*/cmd/*/*-server
capabilities/server/cmd/webhook-sink/webhook-sink
node_modules/
dist/

# Local secrets (never committed)
.env
