// Simplified Chinese for the ERP link's UI (ADR-0023), keyed by the English source text.
import { register } from "@platform/ui";

register("zh-CN", {
  "ERP adapter": "ERP 适配器",
  "ERP orders": "ERP 订单",
});
